# 容器内mysql数据恢复

```
容器内mysql数据损坏
显示 InnoDB: Cannot open table hive_3/cds from the internal data dictionary of InnoDB though the .frm file

可以尝试使用该方法恢复（binlog）
mysqlbinlog --database=hive_3 mysql-replication.000* > recovery0721_hive_3.sql
mysql  -u root -p < recovery0721_hive_3.sql
```
